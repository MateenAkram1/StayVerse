{
  "name": "stayverse",
  "version": "1.0.0",
  "private": true,
  "description": "MERN accommodation booking platform",
  "scripts": {
    "dev": "concurrently -n client,server -c cyan,magenta \"npm run dev --prefix client\" \"npm run dev --prefix server\"",
    "build": "npm run build --prefix client",
    "start": "npm run start --prefix server",
    "seed": "node server/src/seed/seed.js"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  },
  "dependencies": {
    "mongodb": "^7.2.0"
  }
}
